services:
  - type: web
    name: codenote-staff-call
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ADMIN_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
